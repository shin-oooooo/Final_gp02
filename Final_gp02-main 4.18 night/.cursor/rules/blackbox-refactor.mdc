---
description: 黑盒接口守则与文件/函数组织标准（重构不得感染外部调用端）
alwaysApply: true
---

# 黑盒重构与文件/函数组织守则

## 文件 / 函数对应

- 一个文件职责单一；单文件 > ~500 行就考虑拆分成 `<topic>/{contracts,extract,render,…}.py`。
- 一个函数只做一件事；**参数 ≤ 5 个**（超过用 dataclass 或 `Context` 对象聚合）。
- 文件名与主要函数名对齐；外部一看文件名就知道能导入什么。

## 黑盒原则（外部调用者"无感"）

- **外部接口不动**：公开函数名、参数顺序、参数默认值、返回值结构（dict keys / 字段名 / 类型）视为契约。
- **仅改内部**：私有助手（下划线前缀）可任意重命名/拆分/合并；文件路径可挪动，只要公开入口保留（必要时在旧模块里 `from new.path import *` 做桥接）。
- **必须改接口时**：在 PR 说明里**逐条列出**所有受影响的文件 + 行号，并同批修改调用端。

❌ 反例
```python
# 老接口（被很多调用方使用）
def build_bundle(*, rets, r_train, t0_row, h, crash_q, std_q, tail_q, train_tail_days): ...

# 改成单参数 dict —— 破坏所有调用端
def build_bundle(cfg: dict): ...
```

✅ 正例
```python
def build_bundle(*, rets, r_train, t0_row, h, crash_q, std_q, tail_q, train_tail_days):
    # 内部随意重构：抽私有 helper、改实现、拆文件
    plan = _make_plan(rets, r_train, h=h, q=(crash_q, std_q, tail_q))
    return _run_plan(plan, t0_row=t0_row, train_tail_days=train_tail_days)
```

## 返回值字典契约（Snapshot / Bundle）

- 字典一经发布即契约：**只许加字段，不许改名/删字段**；删除需先标 deprecated ≥1 版本，再真正移除。
- 新增字段采用命名空间前缀（如 `fig41_*`、`p4_*`）避免冲突。
- 新增字段必须给默认值，使旧快照仍可被新代码读取。

## 重构后自检

至少跑一次导入与端到端烟测：
```bash
python -c "from dash_app.fig41 import build_fig41, extract_fig41_bundle; print('ok')"
python -c "from research.pipeline import run_pipeline; from research.schemas import Phase0Input, DefensePolicyConfig; \
  snap = run_pipeline(policy=DefensePolicyConfig(), phase0_in=Phase0Input(), sentiment_score=-0.1, sentiment_detail=None).model_dump(); \
  assert 'fig41_verify' in (snap.get('phase3',{}).get('defense_validation') or {}), 'missing fig41_verify'"
```

## 文案/阈值不得硬编码

- 前端文案走 `dash_app/content/*.md`（由 `get_*_label/title/hint/message` 读取）。
- 方法论阈值 / 分位 / 窗口长度走 `DefensePolicyConfig`；参数面板改动需同步 `callbacks/dashboard_pipeline.py` 的 `State(...)` 列表与 `_execute_pipeline_for_dashboard` 的关键字参数。
