## Inputs to the Adaptive Optimizer

1. Per-symbol best-model return and volatility expectations from “shadow model selection” (see Fig2.2) feed the Adaptive Optimizer.
2. Each defense level has different optimization constraints, so optimized portfolio weights differ by level.
