---
description: 模块化 + 接口稳定性编码规范
alwaysApply: true
---

# 模块化 + 接口稳定性

## 1 · 文件与函数

- **单一职责**：每个文件只做一件事；文件名应能一眼看出里面装的是什么。
- **文件规模**：目标 ≤ 300 行；到 500 行必须拆分。
- **函数接口极简**：参数尽量少（>4 个强烈建议用 `@dataclass` 封装）。
- **函数职责单一**：>30 行的函数要考虑拆子函数。

## 2 · 接口稳定性（黑盒原则）

修改已有函数时：

**必须保持不变**：
- 函数名
- 参数顺序 + 默认值
- 返回值类型与结构

**可以自由修改**：
- 函数内部实现
- 私有 helper（`_` 前缀）
- 日志 / 断言

### 例子

```python
# ❌ BAD — 改了参数顺序
def extract_bundle(policy, snap_json):          # 原来是 (snap_json, policy)

# ✅ GOOD — 内部重构，签名不动
def extract_bundle(snap_json, policy):
    # 原来 50 行塞一起；现在拆成 3 个 _helper
    v = _safe_get_verify(snap_json)
    hits = _parse_hits(v)
    return Bundle(hits=hits, ...)
```

## 3 · 确需改接口时的强制动作

如果**必须**改函数名 / 参数顺序 / 返回格式：

1. **先 grep**：列出所有调用者文件 + 行号
2. **在改动说明里列出**所有受影响位置（便于用户审查）
3. **同步改所有调用点**（不留悬挂引用）

示例说明格式：

```
接口变更：`build_fig41_panel()` 返回从 `Dict` 改为 `Fig41Components` dataclass。

受影响位置：
- dash_app/dashboard_face_render.py:384
- dash_app/render/main_p4.py:67
- dash_app/callbacks/research_panels.py:102
全部已同步。
```

## 4 · 拆分检查清单

动刀前先确认：

- [ ] 拆完后每个新文件 ≤ 300 行
- [ ] 拆完后每个公开函数签名**未变**
- [ ] 只有 `_` 私有函数允许改签名
- [ ] 所有调用方已 grep 并列出
- [ ] smoke test 验证 import 链 + 核心返回值

## 5 · "做一轮精简"标准 SOP

当用户说"做一轮精简 / 重构 / 瘦身"时，按此固定流程：

### Step A · 体检（只读）
1. 列出所有 > 500 行的源文件（候选拆分对象）。
2. 列出所有 > 60 行 / 参数 > 5 个的函数（候选简化对象）。
3. 列出疑似死代码：未被任何文件 import 的模块、未被调用的函数（grep 验证）。
4. 列出重复实现：同名/近义 helper 在多文件出现。

### Step B · 分类（决定动刀方式）
| 现象 | 默认动作 |
| --- | --- |
| 文件超长但职责单一 | 抽 `_helper` 私有函数到同文件，公开 API 不动 |
| 文件超长且杂糅多职责 | 拆成 `<topic>/{contracts,extract,render}.py`，原文件留 `from .new import *` 桥接 |
| 函数参数过多 | 内部用 `@dataclass` 聚合，**外部签名一字不改** |
| 重复实现 | 留最稳的一个；其余改为 `from … import …` 转发，**不删旧名** |
| 死代码 | 默认**不删**，先标记 `# DEPRECATED: …`；删除单独提报 |

### Step C · 报告（必须先呈给用户，不要直接动刀大改）
对每条候选给出：
```
[文件 / 函数] · [行数 / 参数数 / 调用次数]
现状：…一句话…
建议：保持外部接口；内部抽 _helper 把 X 段挪走
风险：⬛ 零接口变更 / 🟡 仅内部 / 🔴 必须改签名（列出调用点）
```

### Step D · 执行（用户确认后）
- ⬛ / 🟡 项可批量执行，做完跑一次 import smoke test。
- 🔴 项**逐项**列出 grep 出的全部调用位置，同批修改，再跑端到端 smoke test。

### 红线
- 没列出"受影响位置清单"前，**不许**改任何公开函数签名 / 返回结构 / 字典 key。
- 不许借"精简"之名顺手删 `# DEPRECATED` 之外的任何旧入口。
